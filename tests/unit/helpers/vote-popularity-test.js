import { votePopularity } from 'qa/helpers/vote-popularity';
import { module, test } from 'qunit';

module('Unit | Helper | vote popularity');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = votePopularity([42]);
  assert.ok(result);
});
